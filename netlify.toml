[build]
publish = "dist"
command = "npm run build"
branch = "refs/heads/main"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 20
  
[build.environment]
NODE_VERSION = "18"

[build.cache]
  paths = ["node_modules/**", "package-lock.json"]